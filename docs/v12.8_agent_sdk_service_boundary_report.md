# V12.8: Agent SDK Service Boundary Report

**Date:** 2026-07-02
**Version:** V12.8.0
**Previous:** V12.7 (Writeback Plan / Audit)

---

## 1. Module

`src/services/agentSdkService.ts` — turn pipeline service. No HTTP route. No LLM.

## 2. Service Pipeline

```
createSession → processTurn:
  rawInput → normalize → extract → preview? → policy → context → reply → writeback → AgentTurnResult
```

## 3. Service Methods

| Method | Mutates State | LLM | Description |
|--------|--------------|-----|-------------|
| `createSession` | No | No | Safe defaults |
| `processTurn` | **No** | No | Full turn pipeline |
| `previewTurnEvents` | **No** | No | Preview candidates |
| `prepareWriteback` | **No** | No | Validate writeback plan |
| `applyWriteback` | **Yes** (guarded) | No | Only write method |

## 4. Default No-Mutation

- `processTurn`: `noMutation: true` always
- `applyWriteback`: only method that can write
- Clone by default, `allowMutation: true` for explicit

## 5. Apply Gate

```
policy = apply_allowed/confirmation_required
confirmation = "apply"
candidate exists
→ V11 EventStudioApply boundary
```

Blocked otherwise: `APPLY_BLOCKED` / `CONFIRMATION_REQUIRED` / `NO_CANDIDATE`

## 6. App Contract

`src/appContracts/agentSdk.ts` — types for future HTTP routes, no routes yet

## 7. Test Coverage (15 tests)

| Category | Count |
|----------|-------|
| Session creation | 1 |
| Process turn (pipeline, no-mutation, no-apply) | 4 |
| Preview | 1 |
| Prepare writeback | 1 |
| Apply writeback (block, confirm, succeed, clone) | 5 |
| Service errors / no raw state / no LLM / no multi | 3 |

## 8. Build & Test Verification

```
npm run build        ✅  npm test 2138/2138 ✅
npm run rc:verify    ✅  All gates PASS
agent tests          ✅  150/150 (8 files)
service tests        ✅  15/15
```

## 9. V12 Agent SDK Progress

```
✅ V12.0 Charter   ✅ V12.1 DTOs      ✅ V12.2 Input
✅ V12.3 Extract   ✅ V12.4 Policy    ✅ V12.5 Context
✅ V12.6 Reply     ✅ V12.7 Writeback ✅ V12.8 Service
🔜 V12.9 Static SDK Harness
```

## 10. Next: V12.9 Static SDK Harness

## 11. Files Changed

### New Files
- `src/appContracts/agentSdk.ts` — Service contracts
- `src/services/agentSdkService.ts` — Turn pipeline service
- `tests/services/agentSdkService.test.ts` — 15 tests
- `docs/v12.8_agent_sdk_service_boundary_report.md` — This report

### Modified Files
- `src/core/agent/agentTypes.ts` — Added `ready_for_apply` to WritebackStatus
- `src/core/agent/writebackPlanner.ts` — Fixed type casts
