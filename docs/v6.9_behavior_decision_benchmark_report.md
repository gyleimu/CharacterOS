# CharacterOS V6.9 Behavior Decision Consistency Benchmark Report

## 版本

```text
CharacterOS V6.9 — Behavior Decision Consistency Benchmark
Date: 2026-06-23
Based on: V6.8 Event Impact Benchmark
```

## 测试结果

```text
npm run build        ✓ passed
npm test             ✓ 84 test files, 589 tests, zero failures
npm run next:build   ✓ 17 API routes, zero errors
```

新增 1 assertion helper + 1 fixture + 1 runner + 3 tests。零 V3/V4/V5 行为改动。
behavior_decision fixture 通过真实 physics 执行。

---

## 1. 新增文件

### `src/core/benchmark/behaviorDecisionAssertion.ts`（135 行）

**纯 text/semantic assertion helpers。不调用 LLM。**

| 函数 | 用途 |
|------|------|
| `evaluateTextAssertion` | 评估 text 或 numeric 方向断言 |
| `evaluateTextIncludesAny` | 检查 text 是否包含任一子串 |
| `evaluateTextExcludesAll` | 检查 text 是否排除所有子串 |

### `src/core/benchmark/fixtures/firstReplayFixtures.ts`（新增 Fixture F）

```text
id: wang_xue_returns_after_silence_decision_consistency
category: behavior_decision
events: EVENT_SILENCE_THREE_DAYS
ticks: 3 days
assertions: decision.mostLikelyAction non-empty, confidence ≤ 1,
             behaviorBias count ≥ 1, noiseLevel ≤ 1
```

### `src/core/benchmark/benchmarkRunner.ts`（修改）

**新增 `behavior_decision` category：** `runBehaviorDecisionCase` — 使用 `deriveCharacterState` 获取 decision/biases/embodiedAction，评估 bounded assertions。

---

## 2. All categories supported（6/6）

| Category | Status | Fixture |
|----------|--------|---------|
| `memory_decay` | ✓ | `memory_should_decay_over_time` |
| `homeostasis_recovery` | ✓ | `boundary_should_recover_under_rest` |
| `belief_evolution` | ✓ | `belief_should_strengthen_with_repeated_evidence` |
| `event_impact` | ✓ | `abandonment_event_lowers_trust`, `waiting_event_raises_fear` |
| `behavior_decision` | ✓ | `wang_xue_returns_after_silence_decision_consistency` |

**全部 6 个 category 均已 supported。0 skipped。**

---

## 3. V6 进度

```text
V6.0  Benchmark Design Charter            ✓
V6.1  Benchmark Types                     ✓
V6.2  First Replay Fixtures               ✓
V6.3  Directional Assertion Engine        ✓
V6.4  Memory Decay Benchmark Runner       ✓
V6.5  Homeostasis Recovery Benchmark      ✓
V6.6  Belief Evolution Benchmark          ✓
V6.7  Benchmark Report API                ✓
V6.8  Event Impact Benchmark              ✓
V6.9  Behavior Decision Benchmark         ✓ (本阶段)
V6.10 Benchmark Governance Report         → 下一个
```

## 4. V6 完成（6 categories, 6 fixtures, 589 tests）

```text
CharacterOS V6 Benchmark System — all 5 originally proposed categories
plus behavior_decision are now runnable via benchmark runner.

V6 交付了：
  - 完整的 benchmark type system
  - 6 个 hand-crafted replay fixtures
  - Pure directional assertion engine (directional/bounded/monotonic/invariant)
  - Behavior decision text assertion helper (no LLM)
  - Category-specific benchmark runners with metric resolvers
  - Read-only benchmark report API (GET /api/benchmark/report)
  - 589 tests, zero V3/V4/V5 regressions

V6.10: Benchmark Governance Report — 汇总所有 benchmark 结果和基线.
```

已通过所有测试。
已完成
